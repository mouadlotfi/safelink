# Frontend environment variables

# URL of the backend API service (required; all URL processing logic runs here)
SAFELINK_BACKEND_URL=http://localhost:8000

# Public URL of the website (used in API documentation)
# This should be the URL where your Safelink instance is accessible
NEXT_PUBLIC_WEBSITE_URL=http://localhost:3000
